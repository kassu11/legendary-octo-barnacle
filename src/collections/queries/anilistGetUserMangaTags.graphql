query ($name: String) {
  User(name: $name) {
    id
    name
    statistics {
      manga {
        tags {
          tag {
            id
            name
          }
          count
          meanScore
          minutesWatched
          chaptersRead
          mediaIds
        }
      }
    }
  }
}
